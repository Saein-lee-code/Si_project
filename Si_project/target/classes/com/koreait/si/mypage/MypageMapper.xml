<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.koreait.si.mypage.MypageMapper">
	<update id="updateStd" parameterType="StdPARAM">
		UPDATE t_student SET std_email=#{std_email}, std_phone=#{std_phone}, std_addr=#{std_addr}, std_zip=#{std_zip}
		WHERE std_id=#{std_id}
	</update>
	<update id="updateProf" parameterType="ProfPARAM">
		UPDATE t_professor SET prof_email=#{prof_email}, prof_phone=#{prof_phone}, prof_addr=#{prof_addr}, prof_zip=#{prof_zip}
		WHERE prof_id=#{prof_id}
	</update>
</mapper>
